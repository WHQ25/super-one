# dependencies (bun install)
node_modules

# output
out
download-links.md
dist
dist-terminal
*.tgz

# Generated offline WebView documents (build artifacts)
packages/chat-view/src/generated-host-html.ts
packages/chat-view/src/generated-terminal-html.ts

# code coverage
coverage
*.lcov

# logs
logs
*.log
*.log.old
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo
.wrangler/
apps/desktop/native/computer-use-helper/.build/
apps/desktop/native/computer-use-lab/.build/

# IntelliJ based IDEs
.idea

# Dev data (userData isolation)
.dev-data

# Event trace (dev only). Prefixed too: the LLM proxy writes
# llm-proxy-event-trace.db, which the bare pattern does not match.
*event-trace.db*

# Perf sampling artifacts (dev only)
debug-data/

# Finder (MacOS) folder config
.DS_Store

# Local mini-app data
.superone/

# Playwright artifacts
e2e/.artifacts/
e2e/.report/
playwright-report/
test-results/

# Storybook build output
storybook-static/

# Claude Code local state
.claude/worktrees/
.claude/scheduled_tasks.lock

# Locally installed remotion skill (not used by this repo)
.agents/skills/remotion-best-practices
.claude/skills/remotion-best-practices
skills-lock.json

# WorkBuddy 
.workbuddy

# Grok
.grok

# Temp Docs
docs/temp

# Compiled output of the iOS Simulator helper (built on demand into userData).
apps/desktop/native/ios-simulator-helper/build/
