/**
 * CLI view of the harness catalog. Implementation lives in
 * `@superone/runtime/harness` — this module only preserves the historical
 * import path used across the CLI and its tests.
 */
export { HarnessManager, type HarnessConfigPatch } from '@superone/runtime/harness'
