# Agent instructions (desktop)

Directory guidance lives in sibling [`CLAUDE.md`](./CLAUDE.md)
(Electron main/preload/renderer, IPC, stores, Remote Control, Mini-App, testing, etc.).

Agents that only load `AGENTS.md` and do **not** expand `@` includes **must read**
`./CLAUDE.md` before changing code under `apps/desktop/`.

@CLAUDE.md
