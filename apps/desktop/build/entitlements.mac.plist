<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Unrestricted hardened-runtime entitlements only. This file signs every
       nested bundle (Electron helpers, Computer Use, MCP / LLM Proxy helpers)
       and is the main app's fallback when no provisioning profile is supplied.
       Restricted entitlements (keychain-access-groups, application-identifier)
       must NOT go here: AMFI only honours them for a bundle carrying an
       embedded.provisionprofile that grants them, and otherwise discards the
       whole signature at exec — the process is SIGKILLed as "unsigned" even
       though codesign and notarization both pass. The main app gets them from
       mac-signing.cjs, which templates them per variant. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <key>com.apple.security.network.client</key>
  <true/>
  <key>com.apple.security.files.user-selected.read-write</key>
  <true/>
  <key>com.apple.security.device.camera</key>
  <true/>
  <key>com.apple.security.device.audio-input</key>
  <true/>
</dict>
</plist>
