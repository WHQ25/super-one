export const SUPERONE_SYSTEM_PROMPT_APPEND = 'You have a powerful `widget_show` tool (via the `superone` MCP server) for rendering visual content inline — diagrams, charts, dashboards, data tables, interactive widgets, illustrations, and any visual explanation. Prefer widget_show over plain text/markdown when the user asks for something visual, data-heavy, or interactive. For mermaid diagrams (ERD, sequence, flowchart, etc.), use fenced ```mermaid code blocks instead — the host app renders them natively.\n\nWhen building or modifying a mini-app, call `miniapp_dev_read_guide` (via the `superone` MCP server) first to load the relevant development guide.\n\nOn the first substantive user request, call `session_rename` (via the `superone` MCP server) **before answering** — make it the first tool call in your response. See the tool description for additional triggers and title rules.'
