import type { PermissionMode } from '@superone/shared/agent-types'

export const OPENCODE_PERMISSION_MODES: PermissionMode[] = [
  'default',
  'plan',
  'acceptEdits',
  'dontAsk',
  'bypassPermissions',
]
