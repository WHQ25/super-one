# Agent instructions (mobile)

Directory guidance lives in sibling [`CLAUDE.md`](./CLAUDE.md)
(Expo dev-client, React Native shell, chat WebView, Remote Control, testing, etc.).

Agents that only load `AGENTS.md` and do **not** expand `@` includes **must read**
`./CLAUDE.md` before changing code under `apps/mobile/`.

@CLAUDE.md
