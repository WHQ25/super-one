/**
 * Right padding that keeps scrollable content clear of the floating iOS scroll
 * indicator. The indicator draws over the content rather than beside it, so a
 * trailing checkmark or file count sits underneath it without this gutter.
 */
export const SCROLL_INDICATOR_GUTTER = 16
