远程控制有了真正的文件通道。当你用手机驱动桌面会话时,agent 现在可以把文件直接回传给你。

### mobile_share_file

新增的 MCP 工具让 agent 把任意桌面文件推送到已连接的手机。传输在聊天里以独立工具块呈现,带**实时上传进度**;`shareId` 把工具结果与其数据负载关联起来,大文件也能端到端稳定流传。

### Stat-only 读取

`read_desktop_file` 新增 `statOnly` 模式,agent 可以只取文件的元数据 —— 大小、类型、是否存在 —— 而不必把整个文件拉过线缆。便宜的检查保持便宜。
