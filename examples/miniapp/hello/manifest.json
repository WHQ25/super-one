{
  "appId": "hello",
  "name": "Hello",
  "version": "0.1.0",
  "author": { "name": "SuperOne", "url": "https://github.com/WHQ25/super-one" },
  "description": "A simple greeting sidebar app",
  "logo": "logo.png",
  "type": "sidebar",
  "permissions": { "fs": [{ "scope": "project", "path": ".", "access": "readwrite", "reason": "Read and write project files" }] },
  "tools": [
    {
      "name": "show_message",
      "description": "Display a message in the Hello app",
      "inputSchema": {
        "type": "object",
        "properties": {
          "text": { "type": "string", "description": "The message to display" }
        },
        "required": ["text"]
      }
    }
  ]
}
