{
  "name": "@superone/chat-core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure chat event reducer shared by desktop and Expo.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "fixtures:export": "bun scripts/export-fixtures.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@superone/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^4.0.0"
  }
}
