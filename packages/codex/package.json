{
  "name": "@superone/codex",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Codex harness core — App Server JSON-RPC client + turn (enable with node CLI harness codex).",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@superone/runtime": "workspace:*",
    "@superone/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^4.1.11"
  },
  "engines": {
    "node": ">=20"
  }
}
