{
  "name": "@superone/relay-client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure-TS relay/LAN crypto, ACK, pairing, and RPC for Expo.",
  "exports": {
    ".": "./src/index.ts",
    "./crypto": "./src/crypto.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@noble/ciphers": "2.3.0",
    "@noble/hashes": "2.3.0",
    "@superone/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^4.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
